// app.routes.ts

import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Assurez-vous que le chemin est correct vers votre AppComponent

export const routes: Routes = [
  { path: '', component: AppComponent } // Exemple de route, ajustez selon vos besoins
  // Ajoutez d'autres routes ici si nécessaire
];
